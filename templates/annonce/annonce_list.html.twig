{% extends 'base.admin.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}

    <!-- section -->
    <section class="section section--last">
        <div class="container">
            <!-- content tabs -->
            <div class="tab-content">
                <div class="tab-pane fade show active" id="tab-1" role="tabpanel">
                    <div class="row">
                        <div class="col-12">
                            <div class="table-responsive table-responsive--border">
                                <table class="profile__table">
                                    <thead>
                                    <tr>
                                        <th>Title</th>
                                        <th>Price</th>
                                        <th>Created At</th>
                                        <th>Last Modification</th>
                                        <th>Description</th>
                                        <th>Action</th>
                                    </tr>
                                    </thead>

                                    <tbody>

                                    {% for annonce in annonces %}
                                        <tr>
                                            <td>{{ annonce.title }}</td>
                                            <td>${{ annonce.price }}</td>
                                            <td>{{ annonce.createdAt|date("m/d/Y", "Europe/Paris") }}</td>
                                            <td>{{ annonce.modifiedAt|date("m/d/Y", "Europe/Paris") }}</td>
                                            <td>{{ annonce.description|slice(0, 50) }}</td>
                                            <td>
                                                <a href="{{ path('edit_annonce', {'id': annonce.id}) }}">
                                                    <button class="profile__logout" type="button">Edit</button>
                                                </a>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
            <!-- end content tabs -->
        </div>
    </section>
    <!-- end section -->

{% endblock %}
